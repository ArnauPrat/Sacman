CMAKE_MINIMUM_REQUIRED( VERSION 2.8.3 )
PROJECT( dali )

INCLUDE(FindPkgConfig)
##PKG_SEARCH_MODULE(GLEW REQUIRED glew)
FIND_PACKAGE(OpenGL REQUIRED)
FIND_PACKAGE(GLEW REQUIRED)

INCLUDE_DIRECTORIES(./)
INCLUDE_DIRECTORIES(../)
INCLUDE_DIRECTORIES(${LIB_DIR}/soil/include)
INCLUDE_DIRECTORIES(${LIB_DIR}/jsoncpp)
INCLUDE_DIRECTORIES(${GLEW_INCLUDE_PATH})
message (STATUS ${GLEW_INCLUDE_PATH})

FILE( GLOB_RECURSE SOURCE_FILES "./*" )
ADD_LIBRARY( ${PROJECT_NAME} ${SOURCE_FILES} )
SET( SOIL_LIBRARY ${LIB_DIR}/soil/lib/libSOIL.a)
TARGET_LINK_LIBRARIES( ${PROJECT_NAME} ${SOIL_LIBRARY} ${GLEW_LIBRARY} ${OPENGL_LIBRARIES})
#TARGET_LINK_LIBRARIES( ${PROJECT_NAME} ${GLEW_LIBRARIES})
